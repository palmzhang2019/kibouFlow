    ## 体检总结
    
    ### 各层得分（满分 100）
    - 技术可发现（20分）：19 / 20
    - 页面可理解（30分）：27 / 30
    - 内容可抽取（30分）：26 / 30
    - 内容可引用（20分）：18 / 20
    - 总分：90 / 100
    
    ### 发现的问题清单
    按严重度排序：
    
    #### 错误（必须修复）
    1. 本次未发现阻断级错误（0 项）
    
    #### 警告（建议修复）
    1. 多个页面 meta description 偏短（<40），影响摘要质量与点击率  
       - /zh/guides/paths/framework-japanese-or-job-first（26）
       - /zh/trial（33）
       - /zh/guides/paths/job-prep-cluster-entry（33）
    2. 部分内容页存在“结论/下一步建议”重复标题，影响抽取去重稳定性
    3. “希望整理 vs 路径判断”需要跨页拼接，引用链路偏长
    
    #### 建议（锦上添花）
    1. 增加“希望整理 vs 路径判断”单页对照表
    2. 为问题页补“可执行判定表/决策树”
    3. 为并行策略页补“每周时间配比模板”
    4. 提升首屏摘要块信噪比（减少导航文本干扰）
    5. 为高价值页面提供可复制引用片段
    
    ### 错误优先级分组（用于修复排期）
    - P0（阻断级，优先级最高）：无
    - P1（高优先级）：无
    - P2（中优先级）：无（当前为警告/优化项）
    
    ### 最高优先级错误组详单（如果存在错误则必填）
    - 本次无“错误”级问题，按你的规则不展开错误组实例。
    - 为便于后续机器流程，这里给出“无错误占位结构”：
    
    - group_name: none
    - priority: P2
    - impact_scope: no_blocking_errors_detected
    - why_top_priority: 本次未发现错误级问题，仅有可优化项
    - common_symptom: 部分页面 description 偏短；个别章节标题重复
    - likely_root_cause: 内容模板与编辑规范在元描述长度/章节编排上未完全统一
    - global_fix_strategy:
      - 建立 description 长度与写作模板（40-160）
      - 在文章模板层增加章节去重规则
    - verification_plan:
      - 批量校验全站 description 长度
      - 抽检 10 篇文章验证 H2 去重与引用可抽取性
    - error_instances:
      - （无错误实例）
    
    ### 优先修复建议（Top 5）
    1. 统一修正偏短 description 至 40-90 字并保留结论导向
    2. 清理重复 H2（尤其“下一步建议/结论”重复块）
    3. 在关键问题页补“判断阈值表”（语言能力/岗位清晰度/材料完成度）
    4. 在并行路径页加入“周执行模板 + 示例排期”
    5. 新增“希望整理 vs 路径判断”对照页并强化互链
    
    ### 与上次检查的对比（如果适用）
    - 新增问题：无上次基线，暂无法判定
    - 已修复问题：无上次基线，暂无法判定
    - 持续存在的问题：无上次基线，暂无法判定
    
    ### 机器可解析块（给后续 LLM 直接消费）
    {
      "highest_priority_error_group": {
        "group_name": "none",
        "priority": "P2",
        "impact_scope": "no_blocking_errors_detected",
        "why_top_priority": "本次未发现错误级问题，仅有可优化项",
        "likely_root_cause": "内容模板在 description 长度与章节去重上的编辑规范尚未完全统一",
        "global_fix_strategy": [
          "建立 metadata.description 最低长度与写作模板",
          "在文章模板层做标题去重与结构化摘要规范"
        ],
        "verification_plan": [
          "批量校验 description 长度是否在 40-160",
          "抽检 10 篇文章确认无重复 H2 且引用片段可直接抽取"
        ],
        "error_instances": []
      }
    }

项目体检总结
各层得分（满分 100）
技术可发现（20分）：18 / 20
页面可理解（30分）：27 / 30
内容可抽取（30分）：24 / 30
内容可引用（20分）：16 / 20
总分：85 / 100
执行上下文
工作目录：/Users/palm/PycharmProjects/kibouFlow
是否访问线上网站：否
是否执行构建：阻塞（Node.js 18.20.8 < 要求 20.9.0）
是否执行测试：阻塞（vitest ESM 加载失败，Node 版本不达标）
是否执行 GEO 脚本：通过（成功执行）
阻塞项：Node.js 版本不足；vitest ESM 模块加载失败
发现的问题清单
错误（必须修复）
Node.js 版本不满足要求 — package.json 声明 "engines": { "node": ">=20.9.0" }，当前环境为 18.20.8，导致 npm run build 和 npm run test 均无法执行
vitest.config.ts ESM 加载失败 — ERR_REQUIRE_ESM: require() of ES Module vite/dist/node/index.js not supported，即使 Node 升级后此问题也需修复
警告（建议修复）
sitemap 静态页使用运行时 new Date() — src/app/sitemap.ts 第 18 行 STATIC_SITEMAP_LAST_MODIFIED = new Date() 导致每次构建/运行时更新，lastModified 信号不诚实（GEO 脚本判定为 P0）
带下一步建议的 MDX 覆盖率偏低 — 48 篇 MDX 中仅 25 篇有 next_steps（约 52%），低于 70% 阈值（GEO 脚本 CONTENT_NEXT_STEPS_LOW）
存在过长段落的 MDX 篇数偏多 — 8 篇 MDX 存在超过 200 字的段落（GEO 脚本 CONTENT_LONG_PARAGRAPH_SPARSE）
部分 case 类型文章缺少「先说结论」H2 — 如 content/zh/cases/should-learn-japanese-first.mdx 使用 "## 来访者的原始状态" 而非结论优先结构
建议（锦上添花）
ArticleJsonLd 的 author 字段使用 Person 类型且 name 为 "kibouFlow"（组织名），建议改为真实个人作者名以增强引用可信度
Organization sameAs 仅支持 x.com/github/linkedin，可扩展至 Wikipedia 或 Wikidata QID
Footer 的 cluster 入口链接使用硬编码 slug，建议改为动态生成
错误优先级分组（用于修复排期）
P0（阻断级）：Node.js 版本不满足要求导致无法构建和测试
P1（高优先级）：sitemap static fake lastmod；next_steps 覆盖率不足
P2（中优先级）：长段落问题；case 类型结论结构不一致
最高优先级错误组详单

group_name: "环境与构建阻断"
priority: P0
impact_scope: "整个项目 — 无法执行 npm run build / test"
why_top_priority: "Node 18.20.8 < required 20.9.0，导致所有构建和测试命令失败，项目完全无法验证"
common_symptom: "npm run build 报错 'You are using Node.js 18.20.8. For Next.js, Node.js version >=20.9.0 is required'"
likely_root_cause: "本地 Node 版本未对齐 package.json engines 字段声明"
global_fix_strategy:
  - "方案A（推荐）：使用 nvm 或 fnm 切换到 Node.js >=20.9.0"
  - "方案B：更新 engines 字段允许当前 Node 版本（不推荐，可能引入运行时问题）"
verification_plan:
  - "执行 node -v 确认为 20.9.0+"
  - "执行 npm run build 确认构建成功"
  - "执行 npm run test 确认测试通过"

error_instances:
  - code: "ENV_NODE_VERSION_MISMATCH"
    page_or_file: "package.json:6"
    observed_evidence: "engines.node = '>=20.9.0'，当前 node -v = 18.20.8"
    expected_behavior: "npm run build 和 npm run test 正常执行"
    actionable_fix: "运行 nvm install 20.9.0 && nvm use 20.9.0"

  - code: "VITEST_ESM_LOAD_FAILURE"
    page_or_file: "vitest.config.ts:37"
    observed_evidence: "Error [ERR_REQUIRE_ESM]: require() of ES Module vite/dist/node/index.js not supported"
    expected_behavior: "vitest 能正常加载配置文件"
    actionable_fix: "vitest.config.ts 改用动态 import() 而非 require()，或升级 vitest 到兼容版本"
优先修复建议（Top 5）
升级 Node.js 到 >=20.9.0 — 使用 nvm install 20.9.0 && nvm use 20.9.0，这是所有其他验证的前置条件
修正 sitemap.ts 的 lastModified 策略 — 将 new Date() 替换为环境变量 SITEMAP_STATIC_LASTMOD，避免"伪新鲜"信号
补齐 23 篇缺失 next_steps 的 MDX — 在 ## 下一步建议 区块中提供可操作的 CTA 建议
修复 vitest ESM 加载问题 — 重构 vitest.config.ts 使用 ESM 兼容写法
统一 case 类型文章的结论结构 — 将 "## 来访者的原始状态" 等开头改为 "## 先说结论" 或 "## 結論" 优先结构
与上次检查的对比（如果适用）
新增问题：长段落问题（CONTENT_LONG_PARAGRAPH_SPARSE，8 篇）；next_steps 覆盖率问题（25/48）
已修复问题：无法确认（本次为首次执行本地脚本）
持续存在的问题：sitemap fake lastmod（P0 级别）
机器可解析块

{
  "audit_mode": "project-root-local",
  "model": "minimax-m2.7",
  "highest_priority_error_group": {
    "group_name": "环境与构建阻断",
    "priority": "P0",
    "impact_scope": "整个项目 — npm run build / test 完全无法执行",
    "why_top_priority": "Node 18.20.8 < required 20.9.0，所有构建和测试命令失败",
    "likely_root_cause": "本地 Node 版本未对齐 package.json engines 字段",
    "global_fix_strategy": [
      "nvm install 20.9.0 && nvm use 20.9.0",
      "修复 vitest.config.ts ESM 加载问题"
    ],
    "verification_plan": [
      "node -v 确认为 20.9.0+",
      "npm run build 确认构建成功",
      "npm run test 确认测试通过"
    ],
    "error_instances": [
      {
        "code": "ENV_NODE_VERSION_MISMATCH",
        "page_or_file": "package.json:6",
        "observed_evidence": "engines.node = '>=20.9.0'，当前 node -v = 18.20.8",
        "expected_behavior": "npm run build 和 npm run test 正常执行",
        "actionable_fix": "nvm install 20.9.0 && nvm use 20.9.0"
      },
      {
        "code": "VITEST_ESM_LOAD_FAILURE",
        "page_or_file": "vitest.config.ts:37",
        "observed_evidence": "ERR_REQUIRE_ESM: require() of ES Module vite/dist/node/index.js",
        "expected_behavior": "vitest 能正常加载配置文件",
        "actionable_fix": "vitest.config.ts 改用动态 import() 而非 require()"
      }
    ]
  }
}


